// Schedule block component — Phase 2b stub
// Renders cadence, day picker, time-of-day, duration controls.

export function renderScheduleBlock(state) {
  // Phase 2b: full implementation
  return document.createElement('div');
}
